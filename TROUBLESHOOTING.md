# Troubleshooting

Check `AddComputerToADGroup.log` in `_SMSTSLogPath` or `%WINDIR%\Temp`. Common issues include missing variables, invalid credentials, missing delegation, domain controller reachability, DNS, time synchronization, and LDAPS connectivity.
